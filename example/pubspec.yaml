name: drift_devtools_example_app
description: Simple Flutter app example demonstrating the generated Drift DB
publish_to: none
version: 0.0.0

environment:
  sdk: ">=3.10.4 <4.0.0"
  flutter: ">=3.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  drift_devtools:
    path: ..
  path_provider: ^2.0.15
  path: ^1.8.2

dev_dependencies:
  build_runner: any

flutter:
  uses-material-design: true
