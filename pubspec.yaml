name: drift_devtools
description: "A Flutter package that provides a visual interface for inspecting and debugging Drift databases in real-time."
version: 0.0.15
homepage: https://github.com/ericktarzia/drift_devtools

environment:
  sdk: ^3.10.4
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  sqflite: ^2.4.2
  drift: ^2.32.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
  build_runner: any
  drift_dev: any

flutter:
